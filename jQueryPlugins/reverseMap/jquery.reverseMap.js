/** * jQuery plugin that mirrors a json object, meaning that it returns another object * having the values from the inial object as keys in the new one and vice-versa. *  * @author objeleana */(function($){    $.reverseMap = function(json) {        var map = {};        if (typeof json == "object") {            $.each(json, function(key, value) {                map[value] = key;            });        }        return map;    };})(jQuery);